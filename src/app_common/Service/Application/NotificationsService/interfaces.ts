export enum NotificationType {
    INFO = "info",
    SUCCESS = "success",
    WARNING = "warning",
    ERROR = "error",
}

export enum NotificationPermission {
    GRANTED = "granted",
    DENIED = "denied",
    DEFAULT = "default",
}